# ════════════════════════════════════════════════════════
#  forecastpy_backend/requirements.txt
#
#  CAMBIO: eliminados passlib y bcrypt (incompatibilidad
#  entre bcrypt 4.x y passlib en Python 3.11 Windows).
#  La autenticación usa hashlib SHA-256 de stdlib.
#
#  Instalar:
#    pip install -r requirements.txt
# ════════════════════════════════════════════════════════

# ── ML Core ──────────────────────────────────────────────
tensorflow==2.15.0
scikit-learn==1.4.0
numpy==1.26.3
joblib==1.3.2

# ── Datos ────────────────────────────────────────────────
pandas==2.1.4

# ── API Backend ──────────────────────────────────────────
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.7
pydantic==2.5.3
httpx==0.26.0
aiofiles==23.2.1

# ── Auth JWT (sin bcrypt, sin passlib) ───────────────────
python-jose[cryptography]==3.3.0
# hashlib y hmac vienen incluidos en Python — no instalar nada más

# ── Clima Open-Meteo ─────────────────────────────────────
openmeteo-requests==1.2.0
requests-cache==1.1.1
retry-requests==2.0.0
requests==2.31.0

# ── Reportes PDF ─────────────────────────────────────────
reportlab==4.1.0
matplotlib==3.8.2
pillow==10.2.0

# ── Utilidades ───────────────────────────────────────────
python-dotenv==1.0.0